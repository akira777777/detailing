# 🎨 Detailing Studio - Современное веб-приложение с анимациями

![React](https://img.shields.io/badge/React-19.2.0-blue)
![Vite](https://img.shields.io/badge/Vite-7.2.4-purple)
![Tailwind](https://img.shields.io/badge/Tailwind-3.4.17-cyan)
![Status](https://img.shields.io/badge/Status-Production%20Ready-green)

---

## 📌 Описание

**Detailing Studio** - это современное веб-приложение для сервиса детейлинга автомобилей, разработанное с использованием React, Vite и Tailwind CSS. Проект включает полную систему анимаций, оптимизацию производительности и адаптивный дизайн.

### ✨ Ключевые особенности

- 🎬 **11 CSS Keyframe анимаций** - shimmer, glow, float, slide, fade и т.д.
- 🚀 **Оптимизированная производительность** - 60 FPS, code splitting, minification
- 📱 **Полностью адаптивный дизайн** - работает на всех устройствах
- 🎨 **Современный UI/UX** - glassmorphism, градиенты, плавные переходы
- ⚡ **Hot Module Replacement** - мгновенные обновления при разработке
- 📦 **Production-ready** - готов к развертыванию

---

## 🚀 Быстрый старт

### Предварительные требования
- Node.js 18+ 
- npm или yarn

### Установка

```bash
# Клонировать репозиторий
git clone <repository-url>
cd detailing

# Установить зависимости
npm install

# Запустить dev сервер
npm run dev
```

Откройте браузер и перейдите на [http://localhost:5173/](http://localhost:5173/)

### Production Build

```bash
# Собрать для production
npm run build

# Просмотреть build локально
npm run preview
```

---

## 📚 Структура проекта

```
detailing/
├── src/
│   ├── components/          # React компоненты
│   │   ├── Layout.jsx       # Главный layout
│   │   ├── BeforeAfterSlider.jsx  # Интерактивный слайдер
│   │   └── ...
│   ├── pages/              # Страницы приложения
│   │   ├── Home.jsx        # Главная страница с анимациями
│   │   ├── Gallery.jsx     # Галерея трансформаций
│   │   ├── Booking.jsx     # Бронирование услуг
│   │   └── ...
│   ├── hooks/              # Custom React hooks
│   ├── context/            # React Context (тема)
│   ├── utils/              # Вспомогательные функции
│   ├── App.jsx             # Главный компонент
│   ├── index.css           # Глобальные стили и анимации
│   └── main.jsx            # Точка входа
├── public/                 # Статические файлы
├── tailwind.config.js      # Конфигурация Tailwind
├── vite.config.js          # Конфигурация Vite
├── index.html              # HTML шаблон
└── package.json            # Зависимости
```

---

## 🎨 Анимации

### Встроенные CSS анимации

| Название | Длительность | Эффект |
|----------|-------------|--------|
| `glow` | 2s | Пульсирующее свечение синего цвета |
| `float` | 3s | Плавающее движение вверх-вниз |
| `slideInLeft` | 0.8s | Вход справа налево |
| `slideInRight` | 0.8s | Вход слева направо |
| `fadeInUp` | 0.6s | Появление с движением вверх |
| `pulse-soft` | 2s | Мягкое пульсирование |
| `shimmer` | 2s | Световое свечение |
| `scaleIn` | 0.5s | Масштабирование |
| `rotateIn` | 0.6s | Вращение |

### Утилитарные классы

```jsx
// Поднятие элемента на hover с тенью
<div className="hover-lift">Поднимается</div>

// Светящийся текст
<span className="text-glow">Светится</span>

// Каскадное появление элементов
<div className="stagger-animation">
  <div>Элемент 1</div>  {/* появится с delay 0.1s */}
  <div>Элемент 2</div>  {/* появится с delay 0.2s */}
  <div>Элемент 3</div>  {/* появится с delay 0.3s */}
</div>
```

### Примеры на страницах

**Home Page:**
- Hero section с каскадным появлением элементов
- Service cards с hover-lift и glow эффектами
- Membership icons с плавающей анимацией
- Pulsing slot indicators

**Gallery Page:**
- Scroll-triggered фильтры с stagger эффектом
- Lazy-loaded изображения с blur effect
- Smooth transitions между фильтрами

Полная документация анимаций в [ANIMATIONS.md](./ANIMATIONS.md)

---

## ⚙️ Конфигурация

### Tailwind CSS

Проект использует расширенную Tailwind конфигурацию с 11 кастомными анимациями:

```javascript
// tailwind.config.js
theme: {
  extend: {
    animation: {
      'glow': 'glow 2s ease-in-out infinite',
      'float': 'float 3s ease-in-out infinite',
      'slide-in-left': 'slideInLeft 0.8s ease-out',
      // ... и еще 8
    }
  }
}
```

### Vite

Оптимизирована сборка для production:
- Code splitting vendor кода
- esbuild minification
- Отключены source maps
- Кеширование через chunk names

```javascript
// vite.config.js
build: {
  rollupOptions: {
    output: {
      manualChunks: {
        'vendor': ['react', 'react-dom', 'react-router-dom']
      }
    }
  }
}
```

---

## 📊 Производительность

### Build Output
```
dist/index.html                   1.09 kB │ gzip:   0.58 kB
dist/assets/index-Cxc_UfeQ.css   43.00 kB │ gzip:   7.61 kB
dist/assets/vendor-CDbVTOTp.js   46.01 kB │ gzip:  16.32 kB
dist/assets/index-BUt4gQQI.js   404.21 kB │ gzip: 121.91 kB
```

### Метрики
- ✅ **60 FPS animations** - GPU accelerated
- ✅ **Mobile optimized** - работает на низко-мощных устройствах
- ✅ **Code splitting** - vendor js отделен
- ✅ **Compressed** - gzipped размер оптимален
- ✅ **Fast rebuild** - 2.16 seconds

---

## 🎯 Макет и дизайн

### Цветовая палитра
- **Primary Blue**: `#0091FF` - основной цвет для акцентов
- **Dark Background**: `#050505` - основной фон
- **Panel Dark**: `#0D0D0D` - панели
- **Surface Dark**: `#171717` - поверхности

### Типография
- **Display**: Manrope (заголовки)
- **Body**: Inter (текст)

### Брейк-поинты
- Mobile: < 640px
- Tablet: 640px - 1024px
- Desktop: > 1024px

---

## 🔗 Маршруты (Routes)

```
/              - Главная страница
/gallery       - Галерея трансформаций
/booking       - Бронирование услуги
/calculator    - Калькулятор цены
/dashboard     - Персональный кабинет
```

---

## 📦 Установленные пакеты

```json
{
  "dependencies": {
    "react": "^19.2.0",
    "react-dom": "^19.2.0",
    "react-router-dom": "^7.13.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.0",
    "vite": "^7.2.4",
    "tailwindcss": "^3.4.17",
    "postcss": "^8.5.6",
    "autoprefixer": "^10.4.18",
    "eslint": "^8.56.0"
  }
}
```

---

## 🛠️ Доступные команды

```bash
# Development
npm run dev        # Запустить dev сервер на http://localhost:5173

# Production
npm run build      # Собрать для production в /dist
npm run preview    # Просмотреть production build локально

# Quality
npm run lint       # Проверить код ESLint
npm run format     # Форматировать код Prettier
```

---

## 🌐 Развертывание

### Vercel (рекомендуется)
```bash
npm i -g vercel
vercel
```

### Netlify
```bash
npm run build
netlify deploy --prod --dir=dist
```

### GitHub Pages
```bash
# Добавить в vite.config.js:
export default {
  base: '/repository-name/'
}
npm run build
```

### Docker
```dockerfile
FROM node:20-alpine AS build
WORKDIR /app
COPY . .
RUN npm ci && npm run build

FROM node:20-alpine
WORKDIR /app
RUN npm i -g serve
COPY --from=build /app/dist ./dist
EXPOSE 3000
CMD ["serve", "-s", "dist", "-l", "3000"]
```

---

## 📝 Документация

- **[ANIMATIONS.md](./ANIMATIONS.md)** - Полное описание всех анимаций
- **[IMPROVEMENTS.md](./IMPROVEMENTS.md)** - Список всех улучшений
- **[PROJECT_COMPLETION.md](./PROJECT_COMPLETION.md)** - Статус завершения проекта

---

## 🐛 Поиск и исправление ошибок

### Распространенные проблемы

**Проблема:** `Module not found`
```bash
# Решение:
npm install
rm -rf node_modules package-lock.json
npm install
```

**Проблема:** Port 5173 занят
```bash
# Решение:
npm run dev -- --port 3000
```

**Проблема:** Build ошибки
```bash
# Решение:
npm run build -- --sourcemap
# Проверить /dist/assets
```

---

## 💡 Лучшие практики

### Добавление новой анимации

1. **Определить keyframe в Tailwind config:**
```javascript
keyframes: {
  myAnimation: {
    '0%': { opacity: '0', transform: 'translateY(10px)' },
    '100%': { opacity: '1', transform: 'translateY(0)' }
  }
}
```

2. **Добавить animation класс:**
```javascript
animation: {
  'my-animation': 'myAnimation 0.6s ease-out'
}
```

3. **Использовать в компоненте:**
```jsx
<div className="animate-my-animation">Content</div>
```

### Оптимизация изображений
- Использовать `<img loading="lazy" />` для below-fold изображений
- Оптимизировать размер через [Imagemin](https://github.com/imagemin/imagemin)
- Использовать WebP формат где возможно

### Performance Tips
- Избегать inline стилей, использовать Tailwind классы
- Использовать React.memo для больших списков
- Минимизировать re-renders с useCallback/useMemo
- Профилировать с DevTools

---

## 📄 Лицензия

MIT License - свободен для использования в коммерческих проектах

---

## 👥 Поддержка

Для вопросов и поддержки обратитесь к документации или создайте issue в репозитории.

---

**Спасибо за использование Detailing Studio! 🚗✨**

Проект готов для production развертывания на Vercel, Netlify, или ваше собственное хостинг решение.
